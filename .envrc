#!/usr/bin/env bash
if [ ! -f ./.local/bin/task ]; then
    mkdir -p ./venv/bin
    sh -c "$(curl --location https://taskfile.dev/install.sh)" -- -d -b ./venv/bin
fi

use_nix

# if 
#     PATH="./.local/bin:${PATH}"
